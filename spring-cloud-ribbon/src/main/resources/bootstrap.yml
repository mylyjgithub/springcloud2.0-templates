server:
  port: 58002
spring:
  application:
    name: ribbon
  cloud:
      config:
        uri: http://localhost:58004/        #Config server的uri
        profile: dev                       #指定的环境
        label: master                      #指定分支
